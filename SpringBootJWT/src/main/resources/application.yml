server:
  port: 9090

spring:
  application:
    name: AUTH-SERVICE

  cloud:
    config:
      server:
        git:
          uri: https://github.com/s2taaa/eureka-config-server
          default-label: main
        bootstrap: false